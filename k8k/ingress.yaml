apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms-car-ops-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ms-car-ops-service
                port:
                  number: 8080